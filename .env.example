# AI News Article Query System - Environment Variables

# ============================================================================
# Ollama Configuration
# ============================================================================
# Base URL for Ollama API (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Embedding model to use (REQUIRED: nomic-embed-text)
# This model produces 768-dimensional embeddings
# Install with: ollama pull nomic-embed-text
OLLAMA_MODEL=nomic-embed-text

# Ollama API timeout in seconds (default: 30)
OLLAMA_TIMEOUT=30

# ============================================================================
# Embedding Configuration
# ============================================================================
# Text chunk size for splitting articles (default: 1000)
CHUNK_SIZE=1000

# Overlap between chunks to preserve context (default: 200)
CHUNK_OVERLAP=200

# Batch size for embedding generation (default: 10)
EMBEDDING_BATCH_SIZE=10

# Enable disk caching for embeddings (default: true)
ENABLE_DISK_CACHE=true

# Directory for embedding cache (default: data/embeddings/cache)
CACHE_DIR=data/embeddings/cache

# ============================================================================
# Performance Settings
# ============================================================================
# Number of parallel workers for article extraction (default: 4)
# Increase for faster batch processing, decrease if system resources are limited
MAX_WORKERS=4

# Default number of results for retrieval (default: 5)
TOP_K_DEFAULT=5

# ============================================================================
# Article Extraction
# ============================================================================
# Timeout for article downloads in seconds (default: 30)
ARTICLE_TIMEOUT=30

# Maximum retry attempts for failed downloads (default: 3)
ARTICLE_MAX_RETRIES=3

# Minimum article text length for quality validation (default: 100)
ARTICLE_MIN_TEXT_LENGTH=100

# Directory for storing extracted articles (JSON format)
ARTICLE_CACHE_DIR=data/raw_articles

# ============================================================================
# Vector Store Configuration
# ============================================================================
# Path to FAISS index file
FAISS_INDEX_PATH=data/embeddings/articles.index

# ============================================================================
# Saved States Configuration
# ============================================================================
# Directory for storing saved vector store states
SAVED_STATES_DIR=data/saved_states

# ============================================================================
# LangChain Configuration (Optional)
# ============================================================================
# Enable LangChain tracing for debugging
LANGCHAIN_TRACING_V2=false

# LangChain API key (optional, for LangSmith)
LANGCHAIN_API_KEY=

# ============================================================================
# External APIs (Optional)
# ============================================================================
# News API key for fetching articles (optional)
NEWS_API_KEY=
